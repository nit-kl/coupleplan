{
  "name": "coupleplan",
  "version": "1.0.0",
  "description": "カップル向けWebアプリ「CouplePlan」の開発リポジトリです。  \r 企画書とプロトタイプを起点に、本番実装へ移行するための土台を管理します。",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:html": "htmlhint \"prototype/**/*.html\"",
    "lint": "npm run lint:md && npm run lint:html",
    "api:dev": "tsx apps/api/src/server.ts",
    "api:typecheck": "tsc -p apps/api/tsconfig.json --noEmit",
    "api:smoke": "node scripts/api-smoke-test.mjs",
    "web:typecheck": "tsc -p apps/web/tsconfig.json --noEmit",
    "web:dev": "vite --config apps/web/vite.config.ts",
    "web:build": "vite build --config apps/web/vite.config.ts",
    "web:preview": "vite preview --config apps/web/vite.config.ts",
    "ci:pr": "npm run lint && npm run api:typecheck && npm run web:typecheck && npm run web:build && npm run api:smoke",
    "api:dev:worker": "wrangler dev --config apps/api/wrangler.toml",
    "api:deploy:staging": "wrangler deploy --config apps/api/wrangler.toml --env staging",
    "api:deploy:production": "wrangler deploy --config apps/api/wrangler.toml --env production",
    "d1:migrate:local": "wrangler d1 migrations apply coupleplan-local --local --config apps/api/wrangler.toml",
    "d1:migrate:staging": "wrangler d1 migrations apply coupleplan-staging --remote --config apps/api/wrangler.toml --env staging",
    "d1:migrate:production": "wrangler d1 migrations apply coupleplan --remote --config apps/api/wrangler.toml --env production",
    "p1:cleanup:testdata:staging": "node scripts/cleanup-p1-test-data.mjs --env staging",
    "p1:cleanup:testdata:production": "node scripts/cleanup-p1-test-data.mjs --env production",
    "test": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nit-kl/coupleplan.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/nit-kl/coupleplan/issues"
  },
  "homepage": "https://github.com/nit-kl/coupleplan#readme",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "@types/node": "^25.6.0",
    "htmlhint": "^1.9.2",
    "markdownlint-cli": "^0.48.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.0",
    "hono": "^4.12.15",
    "vite": "^6.4.2"
  }
}
