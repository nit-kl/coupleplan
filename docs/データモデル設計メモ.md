# データモデル設計メモ（P0-5）

最終更新: 2026-04-25

## 目的

MVPで必要なコアエンティティ（`User` / `Couple` / `Invite` を中心）を定義し、Phase 1実装の共通前提を作る。

## スコープ

- 1stリリースMVPの必須フローに必要なデータのみ
- 実DB製品依存の型は未確定（`T3` で決定）
- 企画書にある拡張機能（写真証拠、高度な履歴等）は最小化

## ER（概念）

```mermaid
erDiagram
  User ||--o{ CoupleMember : belongs_to
  Couple ||--o{ CoupleMember : has
  Couple ||--o{ Invite : has
  Couple ||--o{ RouletteSession : has
  RouletteSession ||--o{ RouletteVote : has
  RouletteSession ||--o{ RouletteResult : has
  Couple ||--o{ NinjaLog : has
  NinjaMission ||--o{ NinjaLog : referenced_by
  Couple ||--o{ WeeklySummary : has

  User {
    string id
    string email
    string display_name
    datetime created_at
    datetime updated_at
  }
  Couple {
    string id
    string status
    datetime created_at
    datetime updated_at
  }
  CoupleMember {
    string id
    string couple_id
    string user_id
    string role
    datetime joined_at
  }
  Invite {
    string id
    string couple_id
    string code
    string status
    datetime expires_at
    datetime used_at
  }
  RouletteSession {
    string id
    string couple_id
    string status
    datetime started_at
    datetime finished_at
  }
  RouletteVote {
    string id
    string session_id
    string user_id
    string plan_id
    string vote
    datetime created_at
  }
  RouletteResult {
    string id
    string session_id
    string selected_plan_id
    datetime created_at
  }
  NinjaMission {
    string id
    string title
    int point
    string status
  }
  NinjaLog {
    string id
    string couple_id
    string user_id
    string mission_id
    int point
    string visibility
    datetime created_at
  }
  WeeklySummary {
    string id
    string couple_id
    date week_start
    date week_end
    int user_a_point
    int user_b_point
    datetime published_at
  }
```

## エンティティ補足

- `Couple.status`: `pending` / `active` / `unpaired`
- `Invite.status`: `issued` / `used` / `expired` / `revoked`
- `RouletteSession.status`: `collecting` / `ready` / `decided`
- `RouletteVote.vote`: `like` / `pass`
- `NinjaLog.visibility`: MVPでは `private_until_weekly_release` 固定

## MVPで守る制約

- 1ユーザーが同時に所属できる `active` な `Couple` は1件
- `Invite.code` は有効期間内で一意
- `RouletteResult` は1セッションにつき1件
- 週次公開前の `NinjaLog` は相手へ非表示

## 次アクション

- API境界に合わせて入出力DTOを定義（Phase 1）
- `ADR3`（ルーレット状態）と `ADR5`（週次集計）で状態遷移を詳細化
