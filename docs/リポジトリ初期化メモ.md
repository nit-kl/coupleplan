# リポジトリ初期化メモ（P0-1）

最終更新: 2026-04-25

## 目的

`P0-1`（言語/パッケージ/ディレクトリ方針）の着手内容を、実体のあるメモとして残す。

## 作成した雛形ディレクトリ

- `apps/web`
- `apps/api`
- `packages/shared`
- `infra`
- `scripts`
- `tests/e2e`

## 方針

- 技術選定前のため、実装コードはまだ置かない。
- 先に「アプリ」「共通」「インフラ」「運用」「テスト」の置き場だけ確保した。
- フレームワークや package manager は `docs/開発計画書.md` の `T1`〜`T3` 決定後に固定する。

## 完了条件と判定

| 完了条件 | 判定 |
|----------|------|
| 雛形ディレクトリの作成 | 完了 |
| 言語/パッケージ/構成方針の明文化 | 完了 |
| ルートREADMEで構成方針を共有 | 完了（`README.md`） |

`P0-1` は完了。次は `P0-2`（CI最小）へ進む。
